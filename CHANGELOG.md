# Changelog

All notable changes to this project are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added

- Opt-in motor modules: privacy proxy, context compactor, drift eval, trace miner
- SANDBOX sidecar MVP (`sandbox-runner`) and `execute_code` stub
- TypeScript SDK (`@aicery/sdk`) with SSE streaming
- PLUGIN_PATHS custom tool loading and `inventory-advisor` showcase
- Tiered ROUTER-LLM for `/v1/route`

### Changed

- Public documentation reorganized: `guide/`, `examples/`, `adr/` (removed monolithic `docs/`)

## [0.0.1] - 2026-05-21

Initial open-source release: agent runtime, tools, trace, replay, CLI, Python SDK.
